# Leafmark for VS Code

Arrange Markdown chapters, inspect live word and character counts, edit Leafmark settings, apply themes, run diagnostics, and export PDF, DOCX, or HTML from the Leafmark sidebar.

The extension invokes the configured `@skxv/leafmark` npm package through `npx`. External tools such as Pandoc are still required for exports.
